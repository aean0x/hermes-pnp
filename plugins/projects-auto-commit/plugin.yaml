name: projects-auto-commit
version: 0.2.0
description: >-
  End-of-turn git commit (+ optional push) when $PROJECTS_ROOT (default
  $HERMES_HOME/projects) is a dirty git tree. Disable with PROJECTS_AUTO_COMMIT=0.
author: aean0x
kind: standalone
provides_hooks:
  - post_llm_call
  - on_session_end
