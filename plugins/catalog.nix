# Plugin source catalog. Paths are relative to this file.
# Add a folder under ./<name> and a line here. Same pattern: skills/catalog.nix.
{
  model-router = ./model-router;
  tool-call-coherency = ./tool-call-coherency;
  gbrain-retrieval-reflex = ./gbrain-retrieval-reflex;
  gbrain-memory-flush = ./gbrain-memory-flush;
  secret-handoff = ./secret-handoff;
  projects-auto-commit = ./projects-auto-commit;
}
